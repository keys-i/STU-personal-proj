# Base URL used by the frontend HTTP client (axios wrapper).
# - Keep `/api` if you want “proxy-style” requests (the app calls /api/* and something else forwards it).
# - Set a full URL if the backend is hosted elsewhere.
VITE_API_BASE_URL=/api

# Target for Vite’s dev proxy.
# In `vite.config.ts`, requests to `/api/*` are forwarded here and `/api` is stripped.
# - Local backend: http://localhost:3000
# - If backend is remote: https://your-backend.example.com
VITE_API_PROXY_TARGET=http://localhost:3000
